# Agent Instructions

- Before running `tauri build` or `tauri dev`, check the operating system. If it is not Windows, refuse to run the command and ask the user to run it manually on Windows, because this project currently develops and tests only the Windows version.
